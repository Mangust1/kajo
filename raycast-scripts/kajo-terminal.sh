#!/bin/bash
# @raycast.schemaVersion 1
# @raycast.title Kajo Terminal
# @raycast.mode silent
# Toggle Kajo's drop-down terminal (pinned to the Space it was first opened on).
# Bind a hotkey to this script in Raycast (e.g. ⌃⌥⇧') for quake-style access.
open -g "kajo://terminal"
